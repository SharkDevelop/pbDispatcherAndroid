using Foundation;
using System;
using UIKit;

namespace pbDispatcher.iOS
{
    public partial class CamView : UIView
    {
        public CamView (IntPtr handle) : base (handle)
        {
        }
    }
}